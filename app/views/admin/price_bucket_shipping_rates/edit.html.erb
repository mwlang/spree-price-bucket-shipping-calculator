<h1><%= "#{t("editing")} #{PriceBucketShippingRate.human_name}" %></h1>
<%= error_messages_for :price_bucket_shipping_rate %>
<% form_for(:price_bucket_shipping_rate, :url => object_url, :html => { :method => :put }) do |f| %>
	<%= render :partial => "form", :locals => { :f => f } %>
	<p>
		<%=submit_tag t("actions.update")%>
	</p>
<% end %>